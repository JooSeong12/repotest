우리가 실행하는 프로그램이 메모리에 올라가면 프로세스라고 부른다.
(컨트롤 알트 딜리트 누르고 작업관리자에 들어가보면 프로세스가 보임)

스레드(게임에서 많이 쓰임)
여러가지 동작을 동시에 하기위해 사용하는 것

InputStream = 1글자만 담을 수 있음
InputStreamReader = 배열로 여러글자 담을 수 있음

소켓통신

람다식 문법
하나의 인터페이스를 통해 여러가지의 함수로 활용하는 방식
람다표현식을 사용하는 인터페이스는 메서드가 한개만 있어야한다.
